version: 2.1

orbs:
  maven: circleci/maven@1.0.2


jobs:
  build:
    docker:
      - image: maven
    steps:
      - checkout
   
  test:
    docker:
      - image: maven
    steps:
      - checkout
      - run: /mvn test
  
  
workflows:
  maven_build :
    jobs:
       - build
       - test